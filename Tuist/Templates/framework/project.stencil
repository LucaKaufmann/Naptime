import ProjectDescription
import ProjectDescriptionHelpers

let project = Project(
    name: Feature.{{name}}.rawValue,
    targets: [
        .feature(
            implementation: .{{name}},
            dependencies: [
                .common,
            ],
            resources: [
                "resources/**"
            ]
        )
    ]
)
