import XCTest
@testable import NapTimePersistenceServices

final class NapTimePersistenceServicesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NapTimePersistenceServices().text, "Hello, World!")
    }
}
